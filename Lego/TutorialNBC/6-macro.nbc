#define TURN_TIME   340
#define turn_around \
  OnRev(OUT_C,50)   \
  wait TURN_TIME    \
  OnFwd(OUT_BC,100)

thread main
  OnFwd(OUT_BC,100)
  wait 1000
  turn_around
  wait 2000
  turn_around
  wait 1000
  turn_around
  Off(OUT_BC)
endt

